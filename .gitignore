# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

## Delete below progressively till the end of the project
#backend/app/models/
#backend/app/schemas/
#alembic/             
#alembic.ini          
#backend/app/crud/    
##next commit (crud): product stock management (on saturday)(comment above line and commit with the message)
#backend/app/routers/ 
##next commit (routers): order lifecycle (on monday)(comment above line and commit with the message)
backend/app/config.py
backend/app/database.py 
backend/app/main.py
backend/app/__init__.py
##next commit (all above *.py files): adding database, main and config files (on tuesday)(comment above line and commit with the message)
backend/app/routers/health.py
##next commit (tests): adding health checks (on wednesday)(comment above line and commit with the message)
tests/
##next commit (*.py files): various tests (on Friday)(comment above line and commit with the message)
backend/app/logging_config.py 
##next commit (logging_config.py files): adding logging file (for cloudwatch) (on Friday) (comment above line and commit with the message)  
.env.example    
##next commit (.env.example): adding .env.example file with instructions (on saturday) (comment above line and commit with the message) 
Dockerfile
.github/workflows/ci-cd.yml
ecs/taskdef.json
##next commit (all above three files): adding GitHub Actions CI/CD pipeline      

##next commit (Just remove the very last line in the readme then commit): (on saturday) Completing readme file (comment above line and commit with the message)


# Virtual Environment
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Environment variables
.env
.env.local
.env.*.local

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/
